# Build Fixtures

Each child directory is an independent engine input fixture backed by committed
build and, where enabled, migration goldens. See `docs/testing.md` for the
fixture contract and refresh workflow.
